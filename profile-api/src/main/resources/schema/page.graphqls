extend type Mutation {
    createPage(name: String): Page
    deletePage(id: String): Page
}

extend type Query {
    page(uuid: String): Page
    pages: [Page]
}

type Page {
    id: String,
    name: String
}